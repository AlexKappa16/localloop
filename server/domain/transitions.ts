/**
 * Domain transitions — implemented in LL-102.
 * Scaffold keeps the module so imports resolve during parallel epic work.
 */
export function assertTransitionsScaffolded(): void {
  // no-op placeholder
}
